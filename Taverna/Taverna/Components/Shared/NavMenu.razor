<nav class="clube-main-navbar navbar px-4 h-100 d-flex flex-column justify-content-start align-items-center">
    <div class="container-fluid">
        <NavLink class="navbar-brand w-100 text-center" href="/">
            <span class="text-doofens">CdR</span>
        </NavLink>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="container-fluid d-flex flex-column align-items-start justify-content-center">
            <div id="navbarSupportedContent">
                <ul class="navbar-nav">
                    <li class="text-center d-flex justify-content-center w-100 h-100">
                        <NavLink class="nav-link small-font  w-100" href="user">
                            <span class="w-100">User</span>
                        </NavLink>
                    </li>
                    <li class="text-center d-flex justify-content-center w-100 h-100">
                        <NavLink class="nav-link small-font  w-100" href="kanban">
                            <span class="w-100">Kanban</span>
                        </NavLink>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
